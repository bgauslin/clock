@import 'functions';

// @type String
BUTTON_WIDTH = rem(44)

// @type String
DARK_GREY = #222

// @type String
MAX_WIDTH = rem(512)

// @const String
MENU_ARROW = rem(16)

// @const String
MENU_ITEM_SIZE = rem(32)

// @const String
TRANSITION = .3s ease-out

// @type Hash
Breakpoint = {
  MEDIUM: 'screen and (min-width: %s)' % rem(640)
}
