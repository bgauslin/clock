header
  grid-area header
  padding 0 env(safe-area-inset-right) 0 env(safe-area-inset-left)

  & > div
    align-items center
    display flex
    justify-content space-between
    margin 0 auto
    max-width 'calc(%s + 10vw)' % max-width
    padding 0 5vw

  h1
    font-size var(--header-font-size)
    line-height 1
    margin 0
    white-space nowrap
